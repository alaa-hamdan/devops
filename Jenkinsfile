pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
                sh 'svn --version'
            }
        }
		stage('Test1') {
            steps {
                sh 'node --version'
                sh 'svn --version'
            }
        }
		stage('Test2') {
            steps {
                sh 'node --version'
                sh 'svn --version'
            }
        }
    }
}